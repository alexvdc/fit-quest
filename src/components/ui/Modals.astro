<div id="trainingModal" class="modal-overlay">
    <div class="modal-card">
        <div class="modal-header">
            <span class="modal-subtitle">EXÉCUTION</span>
            <h2 id="modalTitle" class="modal-title">EXERCICE</h2>
        </div>

        <div class="modal-body">
            <p id="modalTutorial" class="modal-tuto">Description...</p>
            
            <div class="modal-objective">
                OBJECTIF : <span id="modalInstruction" class="highlight">--</span>
            </div>

            <!-- Timer ou Compteur -->
            <div id="timerContainer" class="timer-box hidden">
                <div id="modalTimer" class="timer-digits">00:00</div>
            </div>
        </div>

        <div class="modal-footer">
            <button id="btnCancel" class="btn-modal secondary">ANNULER</button>
            <button id="btnComplete" class="btn-modal primary">VALIDER L'EXERCICE</button>
        </div>
    </div>
</div>

<style>
    .modal-overlay {
        position: fixed; inset: 0; z-index: 1000;
        background: rgba(0,0,0,0.85); backdrop-filter: blur(8px);
        display: flex; justify-content: center; align-items: center;
        opacity: 0; pointer-events: none; transition: 0.3s;
    }
    .modal-overlay.active { opacity: 1; pointer-events: all; }

    .modal-card {
        background: #18181b; border: 1px solid rgba(255,255,255,0.1);
        width: 90%; max-width: 400px; padding: 30px; border-radius: 20px;
        box-shadow: 0 20px 50px rgba(0,0,0,0.7); text-align: center;
        transform: translateY(20px); transition: 0.3s;
    }
    .modal-overlay.active .modal-card { transform: translateY(0); }

    .modal-subtitle { color: var(--accent); font-size: 0.8rem; letter-spacing: 2px; font-weight: bold; }
    .modal-title { font-family: 'Rajdhani'; font-size: 2.5rem; line-height: 1; margin: 5px 0 20px; text-transform: uppercase; color: white; }
    
    .modal-tuto { color: #a1a1aa; font-style: italic; margin-bottom: 20px; font-size: 0.95rem; }
    
    .modal-objective { 
        background: rgba(255,255,255,0.05); padding: 15px; border-radius: 10px;
        font-family: 'Rajdhani'; font-weight: 700; color: #fff; margin-bottom: 20px;
    }
    .highlight { color: var(--gold); font-size: 1.2rem; margin-left: 5px; }

    .timer-box { margin-bottom: 20px; }
    .timer-digits { font-family: 'Rajdhani'; font-size: 3rem; font-weight: 900; color: var(--text); }
    .hidden { display: none; }

    .modal-footer { display: flex; gap: 10px; justify-content: center; }
    .btn-modal {
        flex: 1; padding: 15px; border-radius: 10px; font-family: 'Rajdhani'; font-weight: 700; font-size: 1rem;
        cursor: pointer; transition: 0.2s; border: none; text-transform: uppercase;
    }
    .primary { background: var(--success); color: #000; box-shadow: 0 5px 15px rgba(16, 185, 129, 0.3); }
    .primary:hover { transform: translateY(-2px); box-shadow: 0 8px 20px rgba(16, 185, 129, 0.5); }
    .secondary { background: transparent; border: 1px solid #333; color: #888; }
    .secondary:hover { border-color: #666; color: white; }
</style>